# --- Node / build artifacts ---
node_modules/
dist/
*.tsbuildinfo
*.log
npm-debug.log*

# --- ai-sre-service local runtime data (SQLite audit db, WAL) ---
apps/ai-sre-service/data/
*.db
*.db-wal
*.db-shm

# --- Secrets / local env ---
.env
.env.*
!config/examples/*.yaml

# --- Local credentials / access keys (never commit) ---
new-github-accesskey.txt
*accesskey*
*.token
*.pem

# --- OS / editor ---
.DS_Store
.idea/
.vscode/
